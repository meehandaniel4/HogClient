package com.hogv1.module;

/** Declarative module used where vanilla/Fabric hooks provide only display/config behavior. */
public class BasicModule extends Module {
    public BasicModule(String id, String name, String description, Category category) {
        super(id, name, description, category);
    }
}
